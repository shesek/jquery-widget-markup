A tiny (102 bytes) jQuery plugin to create widgets from HTML markup by adding an `data-jq` attribute with the name of the method to execute on the element.

For example:

```html
<a href="#" data-jq="button">Hello, World</a> <!-- executes $(element).button() -->
<input type="text" data-jq="datepicker"> <!-- executes $(element).datepicker() -->
```

Released under [WTFPL](http://sam.zoy.org/wtfpl/COPYING).

See http://www.shesek.info/jquery/create-jquery-ui-widgets-from-html-markup for more details.
